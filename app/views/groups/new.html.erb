<div class="home">
  <%= form_with model: @group, local: true do |form| %>
    <% if @group.errors.any? %>
      <div id = 'errors explanation'>
        <h2>
          <%= pluralize(@group.errors.count, "error")%> prohibited this
          group from being saved:
        </h2>
        <ul>
          <% @group.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <p>
      <%= form.label :'Group Name' %><br>
      <%= form.text_field :name %>
    </p>

    <div class="actions">
      <%= form.submit "Create Group", class: 'btn btn-success' %>
    </div>
  <% end %>
</div>
